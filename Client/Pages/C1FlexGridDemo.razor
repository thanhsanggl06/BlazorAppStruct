@page "/c1-flexgrid-demo"
@inject IJSRuntime JS


<PageTitle>ComponentOne FlexGrid Demo</PageTitle>

<h3>FlexGrid - Basic</h3>
<p class="text-muted">Simple grid with auto-generated columns and paging.</p>

<C1Button Background="C1Color.Cyan" Padding="10" FontSize=20 Click="test"> Test</C1Button>
<C1Button MouseOverBrush="C1Color.FromArgb(255, 100, 150, 255)"
          PressedBrush="C1Color.FromArgb(255, 50, 100, 200)"
          FocusBrush="C1Color.FromArgb(255, 200, 100, 50)"
          Click="@test">
    Nút Tùy chỉnh
</C1Button>

@code {
    private record Person(int Id, string Name, string Email, int Age);
    private void test()
    {
        JS.InvokeVoidAsync("alert", "Test button clicked");
    }
    private List<Person> people = new()
    {
        new(1, "Nguy?n Văn A", "nva@example.com", 25),
        new(2, "Tr?n Th? B", "ttb@example.com", 30),
        new(3, "Lê Văn C", "lvc@example.com", 28),
        new(4, "Ph?m Th? D", "ptd@example.com", 35),
        new(5, "Hoàng Văn E", "hve@example.com", 22)
    };
}
